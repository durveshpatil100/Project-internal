

spring:
  application:
    name: greet-api

  boot:
    admin:
      client:
        url: http://localhost:1111/

#optional if eureka server is already there
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: '*'


